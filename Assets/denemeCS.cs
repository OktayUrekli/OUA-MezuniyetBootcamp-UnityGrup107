using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class denemeCS : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("github denemesi");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
